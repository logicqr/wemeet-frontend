#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "\n🔍 Styling, linting and building your project before pushing..."

# --- Prettier Check ---
echo "🧹 Running Prettier..."
npm run check-format || {
  echo "❌ Prettier check failed."
  echo "💡 Run 'npm run format', stage the changes, and try again."
  exit 1
}

# --- ESLint Check ---
# echo "🧠 Running ESLint..."
# npm run lint || {
#   echo "❌ ESLint check failed."
#   echo "💡 Fix the issues above, stage the changes, and try again."
#   exit 1
# }

# # --- Build Check ---
# echo "🏗️ Building your project..."
# npm run build || {
#   echo "❌ Build failed."
#   echo "💡 Fix the build errors above before pushing."
#   exit 1
# }

# # --- If everything passes ---
# echo "\n✅ All checks passed! Proceeding with commit/push..."
